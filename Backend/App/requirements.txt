# Runtime dependencies for the Dockerized FastAPI app.
#
# Audio transcription is intentionally not installed in the slim image. The
# /audio/record route returns 503 unless those optional dependencies are added.

fastapi==0.115.9
uvicorn==0.34.2
pydantic==2.11.4

openai>=2.37.0
langgraph==0.4.1

chromadb==1.0.8
overrides==7.7.0
typing-extensions==4.15.0
langchain-ollama==0.1.0